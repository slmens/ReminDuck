/* eslint-disable react/prop-types */
/* eslint-disable no-unused-vars */
import React from "react";
import NavBar from "../NavBarComponents/Navbar";

export default function UpdateReminderPage({ id }) {
  // Kartı çek, inputları bunlara göre doldur, düzenlesin, update e bassın, state'de topla, sonra postla

  return (
    <div>
      <NavBar />
    </div>
  );
}
